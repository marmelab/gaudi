applications:
    front:
        type: apache
        links: [app]
        ports:
            80: 80
        volumes:
            php: /var/www
        custom:
            fastCgi: app

    app:
        type: php-fpm
        ports:
            9000: 9000
        volumes:
            php: /var/www
