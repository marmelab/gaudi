{% extends "SandboxMainBundle::skeleton.html.twig" %}

{% block title %}{{ cmfMainContent.title|striptags }}{% endblock %}

{% block content %}
<div class="articleContainer hero-unit">
    <p><em>This is a template used by the sandbox controller</em></p>
    <h1 id="main_title">{{ cmfMainContent.title }}</h1>
    <div>
        {{ cmfMainContent.body|raw }}
    </div>
    <p style="color: green">{{ example }}</p>
</div>
{{ parent() }}
{% endblock %}
